# devsize-plus
A macOS, client-only disk usage explorer (TreeSize-style) that scans a folder and shows where space is going. Built to be **native**, **fast**, and **educational**: Swift + SwiftUI + Swift Concurrency, no servers, no database.
